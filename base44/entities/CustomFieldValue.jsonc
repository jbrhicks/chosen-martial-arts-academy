{
  "name": "CustomFieldValue",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "field_id": {
      "type": "string"
    },
    "field_label": {
      "type": "string"
    },
    "field_value": {
      "type": "string"
    }
  },
  "required": [
    "user_id",
    "field_id"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}