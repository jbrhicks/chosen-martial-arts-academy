{
  "name": "PollVote",
  "type": "object",
  "properties": {
    "post_id": {
      "type": "string"
    },
    "option_index": {
      "type": "number"
    },
    "user_id": {
      "type": "string"
    }
  },
  "required": [
    "post_id",
    "option_index",
    "user_id"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "student"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "data.user_id": "{{user.id}}"
    }
  }
}